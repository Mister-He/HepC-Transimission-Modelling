# =============================================================================
# ci.yml — CI for the HCV PWID compartmental model (R + Rcpp)
#
# Runs on every push / pull request:
#   r-tests            install R packages, compile src/sim.cpp, run
#                      tests/unit and tests/integration
#   python-npe-syntax  syntax-check scripts/npe_train.py
#
# Heavy compute (full multi-start calibration, NPE training) is intentionally
# NOT part of CI; those are run on demand via scripts/run_calibration.R and
# scripts/run_npe.R.
# =============================================================================

name: CI

on:
  push:
    branches: ["**"]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:

  r-tests:
    name: R unit + integration tests
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: "4.4.1"
          use-public-rspm: true

      - name: Install system dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y \
            libcurl4-openssl-dev libssl-dev libxml2-dev libgit2-dev \
            libblas-dev liblapack-dev gfortran

      - name: Install R packages
        run: |
          install.packages(
            c("Rcpp", "RcppArmadillo", "dplyr", "ggplot2", "MASS", "patchwork"),
            repos = "https://cloud.r-project.org", quiet = TRUE
          )
        shell: Rscript {0}

      - name: Compile check (src/sim.cpp)
        run: |
          Rcpp::sourceCpp("src/sim.cpp")
          cat("compile OK\n")
        shell: Rscript {0}

      - name: Unit tests
        run: Rscript scripts/run_tests.R --scope unit

      - name: Integration tests
        run: Rscript scripts/run_tests.R --scope integration

  python-npe-syntax:
    name: Python NPE syntax check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Syntax check npe_train.py
        run: python -m py_compile scripts/npe_train.py
