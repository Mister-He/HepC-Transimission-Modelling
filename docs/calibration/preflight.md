# Preflight record — project redo (revision: v1 progression, 12 parameters)

Date: 2026-08-08. Git SHA: `ad931f0ccbd66021287da5a81d870ae8cb00cfa4`
(working tree carries all current revisions; nothing committed in this
round yet).

## File hashes (recorded before the calibration runs of this revision)

| File | MD5 |
|---|---|
| `src/setup.R` | d8ceea1c1ae5a9521e737848105c87fa (before this revision's edits) |
| `src/sim.cpp` | 5bbbe53985bf2a6f1ad0f6914ea430a7 |
| `Model schematic.pptx` | fa096bb25b30551fb1c4237915f5c704 |

`Model schematic.pptx` is protected and unchanged. `src/sim.cpp` is the
3-strata (D/J/X, 360-compartment) implementation; ageing uses
`y_change = y[base + i] / 10.0 * dt;` (verified present).

## Cleanup (user directive)

All files generated by the previous agent rounds were deleted at the start
of this revision, as instructed: `docs/`, `figures/`, `output/`,
`src/calibration/`, `README.md`, `README.zh-CN.md`. They are regenerated
from scratch in this revision. Pre-existing user deletions of legacy HMC
scripts (`src/HMC_*`, `src/calib.sh`, `src/nelder_mead.R`) were left as-is.

## Baseline specification (this revision)

- Progression rates: `p_NC_CC = 0.027` (Thein 2008);
  `p_CC_DC = 0.0788`, `p_CC_HCC = 0.0479` (Alazawi et al. 2010, PMID
  20497143, untreated-only rates); `p_DC_HCC = 0.0464` (Rivera-Irigoin et
  al. 2006, PMID 17209765, upper 95% CI).
- Transmission: constant (`m_min = m_max = 1`), NOT fitted; absorbed by
  contact-matrix row scales.
- Mortality: 2015 SingStat age-specific rates x PWID SMR omega = 14.68
  (Mathers et al. 2013).
- Fitted parameters: 12 (6 contact scales, 6 beta scales). No
  excess-mortality parameter unless the 60+ group cannot be fitted.
- Soft rule: `beta_scale > 1` per age group (documented if abandoned).
- All figures via ggplot2.

## Baseline (no-change) fit reference

`theta = 0` (all contact and beta scales = 1): NLL 7285.7 (prevalence
RMSE 0.521, population MAPE 1.219, equilibrium pass). Recorded in
`output/calibration/baseline_theta0.csv`. This is the no-change reference
against which all calibration runs are compared.

## Final hashes (after the revision's edits)

| File | MD5 |
|---|---|
| `src/setup.R` | 64f13ff5294b87ae4790170e7080ba11 |
| `src/sim.cpp` | 5bbbe53985bf2a6f1ad0f6914ea430a7 (unchanged) |
| `Model schematic.pptx` | fa096bb25b30551fb1c4237915f5c704 (unchanged) |

Final selected run: `output/calibration/run2_v1_12p_warm/` (NLL 22.44).
