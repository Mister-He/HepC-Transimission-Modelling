# Decision log (append-only)

Every material change to mortality, progression, likelihood,
parameterisation, optimizer settings, or scenario acceptance is recorded
here. Failed runs are kept in `output/calibration/` and referenced, never
deleted.

## 2026-08-08 — Project redo per new prompt.md / AGENTS.md

- Change considered: full project redo with a new baseline specification.
- Reason: user directive — delete all files generated by previous agent
  rounds, adopt the highest defensible progression rates, drop fitted
  `m_min`/`m_max`, prefer `beta_scale > 1`, use ggplot2-only figures,
  avoid new parameters unless the 60+ group forces them, and produce a
  comprehensive project-overview file.
- Actions:
  1. Deleted previous artifacts: `docs/`, `figures/`, `output/`,
     `src/calibration/`, `README.md`, `README.zh-CN.md` (regenerated in
     this revision).
  2. Rewrote `prompt.md` and `AGENTS.md` (new baseline + work plan).
  3. Updated `src/setup.R` progression rates to the highest defensible
     literature values (see entry below) and fixed transmission to
     constant (`m_min = m_max = 1`).
  4. Rebuilt the calibration harness under `src/calibration/`.
- Fit consequence: to be filled by run1_v1_12p (12-parameter fit) and any
  follow-up runs.
- Decision: proceed with the 12-parameter baseline first; invoke the
  excess-mortality contingency only if the 60+ group cannot be fitted.

## 2026-08-08 — Progression rates: highest defensible literature values

- Change considered: `p_CC_DC`, `p_CC_HCC`, `p_DC_HCC` in `src/setup.R`.
- Reason: user directed to re-select progression rates with the highest
  justifiable transition probabilities. Previous values (0.039, 0.014,
  0.014 /yr) were below the HCV-cirrhosis natural-history literature
  (they implied ~19 yr in compensated cirrhosis and ~71 yr from
  decompensation to HCC).
- Evidence:
  - Alazawi et al. 2010, Aliment Pharmacol Ther 32(3):344-55, PMID
    20497143 (systematic review, 13 studies, 2386 HCV-monoinfected
    compensated-cirrhosis patients): decompensation 6.37%/yr pooled,
    7.88%/yr in untreated-only studies; HCC 3.36%/yr pooled, 4.79%/yr in
    untreated-only studies. The model has no treatment (tau = 0), so the
    untreated-only rates are the relevant upper bounds.
  - Rivera-Irigoin et al. 2006, AIDS Res Hum Retroviruses 22(12):1236-41,
    PMID 17209765: HCC in HCV-monoinfected decompensated cirrhosis 3.31
    per 100 person-years, 95% CI 2.70-4.64; upper bound used for
    `p_DC_HCC`.
- Decision: adopt `p_CC_DC = 0.0788`, `p_CC_HCC = 0.0479`,
  `p_DC_HCC = 0.0464` /yr. Genotype-weighted effective rates
  (`rho = 0.78`, GT3 RRs 1.36/1.36/1.93/1.93) are CC->DC 0.101/yr,
  CC->HCC 0.083/yr, DC->HCC 0.080/yr — at/above the upper end of the
  literature ranges for GT3-dominant populations; documented in
  `natural_history_review.md`.

## 2026-08-08 — Remove fitted m_min/m_max (constant transmission)

- Change considered: dropping the two fitted transmission-multiplier
  parameters from the calibration.
- Reason: user directive (AGENTS.md condition 3). The previous fits found
  the multiplier nearly flat (m_min ~ 0.58, m_max ~ 0.57); with constant
  transmission the historical level is absorbed by the contact-matrix row
  scales, removing 2 parameters and any remaining time-anchor dependence.
- Implementation: `m_min = m_max = 1` in `setup.R` (code path retained for
  `sim.cpp` compatibility); the likelihood parameterisation now has 12
  parameters (6 contact + 6 beta).
- Decision: adopted. The equilibrium gate is evaluated over the final 5
  years (T vs T-5) as before.

## 2026-08-08 — beta_scale > 1 soft rule

- Change considered: preferring fitted beta inflow scales above 1 for all
  six age groups.
- Reason: user directive (AGENTS.md condition 2) — modelled annual inflow
  exceeding the CNB-anchored base `beta` values.
- Status: to be resolved by run1_v1_12p and the follow-up constrained
  probe. The rule is abandoned for any age group where it materially
  degrades the acceptance criteria; this entry is updated with the outcome.

## 2026-08-08 — 12-parameter fit: run1 and the warm-start basin

- Change considered: adequacy of the 12-parameter baseline (6 contact + 6
  beta scales, constant transmission, no excess mortality).
- Evidence (run1_v1_12p, 9 starts x maxit 3000): best NLL 31.33
  (start population_informed_b6_c1). Acceptance criteria NOT met:
  prevalence RMSE 0.0305 (criterion 0.02), max |prev err| 0.0698 (<20
  overshoot, criterion 0.03), population MAPE 0.1246 (0.10), max APE 0.411
  (40-49, criterion 0.20). The 60+ group itself was nearly fitted
  (p6 0.379 vs 0.3545; N6 409.3 vs 409).
- Diagnosis: Nelder-Mead missed a better basin. A warm start derived from
  the previous round's converged 15-parameter solution (transmission
  multiplier m ~ 0.57 merged into the contact row scales, beta scales kept,
  eta_s6 dropped) evaluates at NLL 29.2 with populations (99, 1243, 1471,
  1843, 1480, 460) and prevalence (0.101, 0.160, 0.249, 0.406, 0.453,
  0.371).
- Polish result (Nelder-Mead, maxit 6000, seed 101, from that warm start):
  **NLL 22.47**, N = (99.3, 1243, 1470, 1862, 1424, 463), p_sero =
  (0.112, 0.173, 0.268, 0.431, 0.478, 0.374); prevalence RMSE 0.008,
  max |prev err| 0.019, population MAPE 0.046, max APE 0.132, equilibrium
  pass (max |prev change| 0.00124). **All acceptance criteria met with the
  12-parameter baseline and the adopted progression rates.**
- Decision: add the run7-derived warm start (and its converged point) to
  the multi-start set in the harness; run2_v1_12p_warm re-establishes
  multi-start stability. No excess-mortality parameter is needed (the 60+
  group is fitted within criteria); the contingency is not activated.

## 2026-08-08 — beta_scale > 1 outcome (soft rule)

- Result: the 12-parameter fit that meets all acceptance criteria has
  beta_scale = (0.169, 0.911, 1.283, 5.435, 15.135, 112.264). Groups 1-2
  (<20, 20-29) are below 1; groups 3-6 are above 1.
- Reason: the <20 and 20-29 prison populations (99 and 1244) are small,
  while the CNB-anchored base beta values are large (257 and 644/yr); the
  steady-state balance needs net inflows of ~43 and ~587/yr respectively,
  i.e. scales of 0.17 and 0.91. Forcing scales >= 1 would require inflows
  >= 257 and >= 644/yr and would overshoot the young prison populations
  several-fold (documented by the constrained probe
  output/calibration/probe_beta_constraint_*/beta_constraint_compare.csv).
- Decision: per the user's soft rule, keep the unconstrained fit; the rule
  is abandoned for groups 1-2 with this documented reason. Absolute
  modelled inflows: ~43, ~587, ~375, ~511, ~318, ~112/yr (total ~1946/yr,
  exceeding the CNB-anchored base total of 1309/yr).

## 2026-08-08 — run2_v1_12p_warm (final) and beta>1 constrained probe

- Run2 (11 starts: 1 zero + 3 informed + 2 warm + 5 perturbed, maxit
  3000, seed 101): best NLL **22.436** (start warm_12p_22p5); the second
  warm start (warm_run7_derived_12p) reproduces the basin at 22.478.
  Acceptance metrics: prevalence RMSE 0.0084, max |prev err| 0.0195,
  population MAPE 0.0474, max APE 0.1263, equilibrium pass
  (max |prev change| 0.00125). Laplace (1000 feasible draws, effective
  dimension 11/12): all six age groups overlap the observed intervals for
  both prevalence and population (60+ p [0.323, 0.417] vs [0.309, 0.402];
  N [407, 558] vs [336, 498]).
- Constrained probe (`probe_beta_constraint_run2`): with beta_scale > 1
  forced for all groups, NLL rises to 184.06 (population MAPE 0.914, max
  APE 4.92). The constraint cannot be satisfied while meeting the
  acceptance criteria; the soft rule is abandoned for groups 1-2 (and the
  boundary pushes group 3 to 1.0 in the constrained fit, confirming the
  unconstrained 1.263 is the right trade-off).
- Decision: **run2_v1_12p_warm is the final selected fit.** No
  excess-mortality parameter (contingency not activated). Final outputs,
  figures, and reports written.

## 2026-08-09 — Bayesian phase: priors, sampler, and multi-start MCMC

- Change considered: posterior sampling for the 12 fitted parameters and
  Bayesian credible intervals for the 12 target summaries, on top of the
  NM fit (run2_v1_12p_warm). Instructions: `prompt_mcmc.md`.
- Priors (documented in bayes_methodology.md): log contact scales ~
  Normal(0, 2^2) (base contact matrix is a model-specific calibrated
  guess); log beta scales ~ Student-t(3, 0, 2) (base beta is
  CNB-official-anchored but per-age values are placeholders; heavy tails
  tolerate the data-driven 60+ inflow). Priors are NOT constructed from
  the NM fit (no double counting); NM solutions are only chain starts and
  proposal tuning.
- Sampler: adaptive Metropolis-Hastings (Haario et al. 2001), proposal
  covariance initialised from the Laplace covariance (eigenvalue cutoff
  1e-4). HMC not used: numerical gradients cost ~12 simulations per
  gradient, making HMC ~10x more expensive per effective sample; NPE
  would require a Python simulation interface and training data without
  benefit over direct MCMC (documented in bayes_methodology.md).
- Multi-start chains: 3 chains from warm_12p_22p5 (NLL 22.44),
  warm_run7_derived_12p (22.48), population_informed_b6_c1 (31.33);
  20000 iters, burnin 5000, thin 5, seed 2026.
- Convergence (mcmc_bayes/diagnostics.csv): R-hat 1.0007-1.0480 (mpsrf
  1.011), acceptance 0.247-0.277, per-chain ESS 88-434 (theta12 lowest,
  pooled ~670). Posterior median vs NM max abs diff 0.695 at theta6
  (contact row 6) — the weakly identified contact6/beta6 trade-off
  direction that Laplace truncates (effective dim 11/12); MCMC keeps its
  full marginal uncertainty under the prior.
- Results (credible_intervals.csv): posterior predictive medians
  (60+ p 0.379, N 450; beta6 scale median 107, CrI [43, 173]). All 12
  target summaries' MCMC 95% CrIs overlap both the observed intervals and
  the Laplace 95% CIs.
- Decision: adopt the MCMC posterior as the primary Bayesian uncertainty
  summary alongside the Laplace intervals; document both and their
  differences in bayes_methodology.md and final_report.md.

## 2026-08-09 (revised) — NPE primary attempt, strict criteria, fallback

- Change considered: per user directive, the Bayesian phase was redone with
  NPE/SNPE as the PRIMARY method, stricter convergence rules (R-hat in
  [0.99, 1.01]; ESS > 400), and the previous round's MCMC outputs deleted.
- NPE attempt: sbi 0.27 `NPE_C` (MAF), 3 sequential rounds (60,000 prior
  draws + 2 x 20,000 proposal draws), two independent seeds (2026, 2027),
  60,000 posterior draws per seed; SBC on 298 held-out prior points.
- NPE diagnostics: SBC coverage acceptable (95% band 0.94-1.00), but the
  posterior is (a) overconfident in well-identified directions — <20
  prevalence CrI [0.106, 0.116] vs MCMC [0.055, 0.174], 20-29 population
  [1232, 1263] vs [1035, 1529] — and (b) unstable across seeds in weakly
  identified directions (theta6 2.5% quantile -4.21 vs -19.63; predictive
  60+ population median 521 vs 491).
- Fallback triggered per prompt_mcmc.md rule 1: the traditional MCMC
  validation becomes the PRIMARY Bayesian summary. Adaptive Metropolis,
  3 chains from NPE median / NM best / informed NM start, block warm
  restart; final 40,000 iterations, burnin 5,000, thin 5.
- Strict criteria met: R-hat 1.0008-1.0096 (all in [0.99, 1.01]; mpsrf
  1.007); ESS pooled 1,374-2,457 (> 400 per parameter; per-chain 332-855);
  acceptance 0.238.
- Final results: MCMC posterior predictive 95% CrIs overlap observed and
  Laplace intervals for all 12 targets (60+ prevalence 0.379, CrI
  [0.336, 0.424]; population 448, [381, 530]). NPE retained as a fully
  documented sensitivity (posterior_samples_npe*.csv, sbc_summary*.csv,
  fig_density.png).
- Decision: adopt MCMC as the primary Bayesian uncertainty summary (strict
  criteria met); keep NPE results and the fallback rationale in
  bayes_methodology.md.
